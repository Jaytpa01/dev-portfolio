---
import TextLink from "@components/TextLink.astro";

interface Props {
	id?: string;
}

const { id } = Astro.props;
---

<TextLink href={id ? `#${id}` : undefined}>
	<h2
		class="pb-2 text-lg font-bold before:pr-1 before:content-['#']"
		{id}
	>
		<slot />
	</h2>
</TextLink>
